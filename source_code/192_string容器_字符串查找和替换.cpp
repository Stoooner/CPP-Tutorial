#include <iostream>
using namespace std;
#include <unistd.h>

/*
    find和rfind的区别就是find是从左往右进行查找，返回查找到的字符串所在的index，如果没有找到就返回-1
    而rfind则是从右往左进行查找，找到的index也是按照从左往右的index返回，只不过查找的顺序是从右往左而已
    可以利用str.find("de")==str.find("de")来判断字符串"de"是否在整个字符串中只出现一次
*/

void test01(){
    
}

int main(){
    test01();
    pause();
    return 0;
}