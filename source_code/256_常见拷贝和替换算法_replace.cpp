#include <iostream>
using namespace std;
#include <unistd.h>
#include <vector>
#include <algorithm>
/*
    replace(iterator beg, iterator end, oldvalue, newvalue)
    // 将区间中的所有指定的旧元素都替换为新元素
*/

void test01(){
    
}

int main(){
    test01();
    pause();
    return 0;
}