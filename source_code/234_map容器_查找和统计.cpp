#include <iostream>
using namespace std;
#include <unistd.h>

/*
    对map容器中的数据进行查找和删除
        - find(key); 查找key是否存在，若存在则返回该键对应的元素的迭代器，若不存在，则返回mp.end();
        - count(key); 统计key的元素个数
*/

void test01(){
    
}

int main(){
    test01();
    pause();
    return 0;
}